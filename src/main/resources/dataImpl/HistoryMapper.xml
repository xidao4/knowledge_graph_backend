<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sec.supernatural.backend_coin.data.HistoryMapper">

    <select id="find10LatestKeyword" resultMap="History">
        select keyword from History where userId = #{userId}
        order by createTime desc limit 10
    </select>

    <insert id="addHistory" parameterType="com.sec.supernatural.backend_coin.po.History" useGeneratedKeys="true" keyProperty="historyId">
        insert into History (userId, keyword, createTime) VALUES
        (#{userId}, #{keyword}, #{createTime})
    </insert>

    <resultMap id="History" type="com.sec.supernatural.backend_coin.po.History">
        <id column="historyId" property="historyId"></id>
        <result column="userId" property="userId"></result>
        <result column="keyword" property="keyword"></result>
        <result column="createTime" property="createTime"></result>
    </resultMap>
</mapper>
